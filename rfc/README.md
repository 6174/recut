# rfc/

> L2 | 父级: /README.md

成员清单
2026-09-23-local-generation-studio.md: 本地模型本地生成（Generation Studio）——基于本机模型、**一个 runtime 托管多个模型（ComfyUI 式）**的图片/视频生成内置 App（`recut.gen-studio`，`apps/gen-studio`）。以 audio-studio 协议同构落地：**内置分发**（`distribution.builtin` + 嵌入 tar.gz + daemon 启动原子替换，升级客户端即升级 App，开发软链接不被覆盖）；**runtime/model 分离**（注册表 `python/registry.json` 分 `runtimes`（依赖闭包 + 专属 venv，默认一个 `diffusers` runtime 托管所有图片模型）与 `models`（权重 + 归属 runtime + `formSchema` 表单 schema）；`bootstrap.py` 从 `RECUT_VENV` 派生 `<fp>-diffusers` runtime venv，重依赖与轻量主 venv 隔离；**换模型只下权重、不换环境，只有依赖冲突才加 runtime**）；**环境准备与模型下载分离**（`gen.prepare { target }` 只建 runtime/venv、`gen.install { model, source }` 只下权重，各自独立可重试，权重源 huggingface/modelscope/automatic 可选）；**异步任务同构**（`gen_tasks` 账本 + `pumpQueue`：generate 单槽 FIFO、prepare 单槽等推理排空、install 并行；提交永不拒绝、`recut.job.*` 统一观察、日志落文件、可取消）；**交互**（Left 两 Tab：① 生成表单——顶部模型切换器 + 按 `formSchema` 渲染的每模型表单；② 记录——环境/下载/生成统一任务列表；Right 统一生产预览 + 进度日志，点任一行看对应结果或日志；复用 audio-studio 任务中心 UX）；**平台接入（App 声明式，无 per-app 代码）**（App 在 manifest `contributes.media` 静态声明本地 provider `local-gen` 与模型 `qwen-image`；平台新增 `service/media/app_providers.go`（`RegisterAppProviders` 把贡献 provider 并入全局目录、CDN 刷新不丢）与通用执行桥 `service/app_media_bridge.go`（遍历已安装 App 的贡献、按 provider id 分派：`gen.generate`→等 shell job→授权 `gen.save`→回读平台 Asset→挂项目；动态就绪面调 App 的 `gen.catalog`）；`execute()` 本地 provider 按 id 分派、`configuredModelFor`/`mediaReadiness` 的 `local-audio` 硬编码泛化为 `Protocol=="local"`；新增 manifest 契约字段需同步 `docs/app-contract.md`）；**全局生图选择本地环境**（把 `image.generate.default` 路由指向 `local-gen/<model>`，或单次传 `modelId`，均复用既有 route/无凭据本地路由）；**可发现 MCP**（App 侧 `gen.catalog`/`gen.status`，平台侧 `recut.media.list_capability_models` + `GET /v1/media/capabilities/{capability}/models` 聚合本地生成 provider，镜像 `CapabilityVoiceGroups`）。第一版兑现 Qwen-Image（`Qwen/Qwen-Image-2.1`）文生图；附契约改动清单、M0–M4 里程碑与验收。不新增渲染引擎、不引入中间语言、不做节点式编辑器、不改提案/路由门禁。
2026-09-24-modal-functions.md: Modal 云函数（Modal Functions）——把开源 GPU 项目托管到 modal.com 的内置 App（`recut.modal-studio`，`apps/modal-studio`），解决「很多开源项目要显卡、用户本机没有 GPU」：modal.com 每月 $30 免费算力（用户可开多账号）。**一个 App、多个预设包（recipe）**：每个 recipe 是自包含目录（`manifest.json` 声明 image/volume/functions/表单 + `modal_app.py` 云端 Modal App + `bootstrap.py` 准备 volume），加能力 = 加目录。**本机薄客户端、不当 HTTP 也能用**：本地只跑 `python/modal_runner.py`，用 Modal SDK `Function.from_name(app, fn).with_options(gpu=...).remote()` 把云端函数当本机 python 函数调用；HTTP endpoint 仅可选。**环境/权重分离（对齐 gen-studio）**：runtime(venv)↔**Modal Image**、model(权重)↔**Modal Volume**、prepare↔`modal.deploy`、install↔`modal.install`(bootstrap 把权重下进 Volume)、generate↔`modal.generate`（单槽 FIFO）。**token / 多账号**：`modal.profiles.add/list/remove`（token 只存本机、0600、不进日志，支持多个 modal 账号绑定不同 recipe）；`modal.secret.set` 写用户账号下的 Modal Secret（HF token）。**平台接线只做两处小泛化**：① `contributes.media` 允许 `protocol:"app"`（App 代执行的远程/付费 provider，与 `local` 共用 `app_media_bridge.go` 通用执行桥，`isAppExecutedMediaProvider` 泛化 `execute()`/`configuredModelFor`/`mediaReadiness`/`capability_models`）；② `ContributedMediaModel` 透传 `Parameters/ReferenceFields/EditModelID/RequiresProposal`（Modal 每函数一张表单 + 视频确认门 + `WaitMinutes` 长任务窗口）。**交互**（Left 两 Tab：功能表单/统一记录 + Right 预览日志 + Setup 门填 token，与 gen-studio 同源；表单由 recipe `formSchema` 驱动，GPU 档位可切）。**路由**：`video.generate.default` 可指向 `modal/<model>`，经提案确认门 → 云端出片 → 平台 Asset，不再依赖 atlascloud 等平台代持凭据；`recut.media.list_capability_models` 聚合 `app` provider 并标计费提示。第一版兑现 MiniMax H3 视频 + 一个图片 recipe；附契约改动清单、M0–M4 里程碑、验收与风险（等待窗口/日志流式/产物回传/额度计费/recipe 供应链）。
2026-09-23-editor-agent-toolchain-session-retro.md: 编辑器 Agent 工具链成片体验复盘（一次「覆盖全部编辑器 MCP tool」的真实会话：93 分钟、工具几乎跑遍，却因六类契约缺口从未 `export.start`，用户手动取消）。把 17 项体验缺陷归并为六个架构主题——① **默认值必须可交付**：`editorCoreDefaultAudioParams.volume:0` 致 6 条旁白全程静音、逐条手改 → 默认改 1 + `timeline.placeAudio` 增 `volume` + 静音**软提示**（不拦导出）；② **派生物可重算**：字幕与 speech 无绑定、改口播后删整条字幕轨重导 → cue 可选 `sourceRef` + 新增 `subtitle.resync`（轻量版，不做硬 desync 校验）；`subtitle.generate` 升级整轨 ASR；③ **工具契约必须可验证 + 默认直通**：`component.create` 返回草稿却按成功返回、逐个 `motion-graphic.update` 救 → **create 默认直通**（传 `source` 一步完成构建+验证→verified，不再默认拉受限作者子 Agent），作者子 Agent 收敛为显式参数（`author:true`/`mode:"agent"`）用于复杂组件；返回结构化明细（不新增 finalize op）；本地路由 `recut.speech.generate` 在 `audio.save` 步失败（`local speech save failed: transport`）且占 Audio Studio 单槽、产生重复任务 → 结构化失败 + 幂等去重 + 不占槽 + 端到端测试；④ **写入身份与并发要稳定**：`script.clean` delete+insert 重建语音元素致 `element.get` not found、内部副作用 op 让 `placeAudio` 的 baseVersion 意外过期、`film.package.import` 污染主轨、`effect.*` 被当组件放置（`component-def`）→ trim/split 保 id + 返回 remap；写入口冲突返回 `retryWith`（不自动重试）；`film.package.import` 默认落新场景；`effect.*` 经组件入口拒绝（数据正确性）；⑤ **验证=交付**：iframe 心跳门禁致 03:15–04:06 共 50 分钟无画面验证 → 过渡期 stale 缓存帧 + 保心跳（无头本体归既有 native-migration RFC，不重复设计）；组件多 progress 拼图（复用 contact-sheet，只加 progress + 角标）；组件 seek 不安全模式构建期 **warning** 提示（非硬拦）。**⑥ 生成必先导演（Prompt 引导，不加代码门禁）**：`recut/SKILL.md:96-98` 已写硬约束，但会话的 `requiredSkill` 是 `recut-editor`、Agent 没读到 → 把"先 `recut-director`"**镜像到 editor 生成段落 / 工具描述**、Plan 模板加导演段（route / viewer job / 媒介 / 段·场景 / STYLE LOCK / references）、`requiredSkill` 含 `recut-director`；**不**新增 planRef 校验/拒绝、不收紧 `mode:"generate"`、不做 provenance 告警。附契约改动清单、M0–M4 里程碑与"救火操作数 ≥14 → ≤2"的元验收口径。不新增渲染引擎、不引入中间语言。
2026-09-22-clone-director-generation.md: 克隆的导演化生成（从「一图一视频」到分镜驱动的连续场景）：以一次真实会话（project `c8323fb54bec736787c826fc` / World `6ab9ebc7ec45066f91c1c5dc`）复盘定位六类根因（定位错误把生成当素材装配线、无导演过程、参考与角色声线接线断裂、计划载体太薄、无 G2 计划门、平台/工具契约缺口、续跑不校验参考身份）。核心判断：**克隆 = 读懂参考 → 重新导演一支新片 → 用连续生成兑现；AI 生成的素材单位是可被导演的「场景/连续段」，不是一帧/一段 5s/一条音**。① 补导演段：`remix → Treatment → shot → generation-prompt`，把 `references/shot`/`generation-prompt` 设为必经；② 素材单位改为段/场景，一段戏用**一次多镜连续生成**（Seedance 15s/多参考/首尾帧/`videoAssetIds` 续接）+**一图分镜表 + 分镜脚本 + 首尾帧合同**兑现，取代硬拼接；③ 中间协议收敛为 AI 直读的 markdown（BRIEF/reference/TREATMENT/PLAN/PROGRESS，意义优先、拒绝 XML/SVML 与字段表）；④ 场景媒介判定（generate/component/typography/hybrid/supplied）并接 `recut-motion-graphic` 与上屏文案系统；⑤ 参考/声线全接线：从 `recut.worlds.get.references[]` role 表建锚定、视频走 `references`+`audioAssetIds`、**World `character` 增 `voice_reference` 字段并修 `world.md` 指引**、修 `recut-clone/SKILL.md:87` 的图片专用措辞；⑥ 生成政策、G1–G5（**新增 G2 计划门**，明确视频确认是第二个人工门）、平台/工具契约修复（无 target 报错、生成 VO 锚点、禁止 mtime 猜文件、产物归属、ASR/TTS 预检）、技能改动清单与 M0–M4 里程碑。不新增渲染引擎、不引入 XML/DSL。
2026-09-20-video-script-storyboard-sheet.md: 视频脚本与一图分镜表：视频创作的核心是**视频故事 + 视频脚本**，而分镜是把脚本落成画面的桥。① 新增 `script`（视频脚本）预设实体类型（locked 字段 `logline/beats/vo/durationSec/aspectRatio/platform/storyboard` + `background`），与 `story` 以 `script_of` 关系分离「叙事内核」与「可生成规格」；② 定义**一图 N 宫格分镜表（storyboard sheet）协议**——默认 5×5=25 格、严格等分 + 细黑缝 + 每格 `R{r}C{c}` 坐标与镜号，配一份不含在图内的 panel manifest（景别/动作/时长/节拍/refs），切格退化为确定性像素运算；③ 引导动作改造：`story.storyboard` 从「6–8 格草图」改为「一张 N 宫格 sheet + manifest」，新增 `story.script` 与 `script.*` 动作组（storyboard/panels/shotlist/vo/keyframes/videos/consistency），`cards.ts` 增 `STORYBOARD_SHEET_LAYOUT` 单一版式来源；④ 切格 → 细化 → 视频流水线：新增平台原语 `recut.media.gridSlice`（ffmpeg crop，返回 N 格 assetId+region）与受控 role `storyboard`（image，三处镜像同步），逐格以宫格为构图锚点重生成关键帧再落视频提案；⑤ Director/Skill 引导：`references/shot` 增「一图分镜表（宫格压缩法）」与 `assets/storyboard-sheet-template.md`、`generation-prompt` 补 role/锚定规则、`short-drama` 补交接、`recut-director` Mode 表加 `script` 环节。含风险（模型格数稳定/单格分辨率/切格错位/role 漂移）与 M1–M4 里程碑。
2026-09-18-editor-text-library.md: Editor 文本资源库：把「文本」Tab 从单一「默认文本」升级为二级分类的文本库，两条实现路径——**原生文本样式预设**（`text/presets.ts`，17 个：标题/正文/引用/标签/列表/标注，插入一个带完整样式的 text 元素，可拖拽、可走样式与文字动画）与**组合型文本组件**（`runtime/components/text-library.tsx`，9 个：标题组合/高亮标题/引用卡片/箭头标注/序号列表/流程步骤/数据大字/人物条/标签徽章，react surface 单对象多段排版）。`ComponentDefinition` 新增 `textGroup` 标记归属（只进文本面板，组件面板过滤），面板对齐特效/组件 Tab 的左侧分类 rail + 网格；`TextDragData` 扩展 `params` 让拖拽保留样式。含 P0 清单、分类词表、边界（字幕/组件/属性面板）与分期。
2026-09-17-editor-native-migration.md: Editor 迁移（并入 web 统一前端 `timeline-editor` + timeline 逻辑下沉 Go + 组件/素材平台化 + 平台 Render Host）：三件迁移合并为一条路线，**先于视频理解/素材 attrs 协议层/clone skill** 三步。① **UI 并入 web 单 React 树**（无 iframe，模块名 `timeline-editor`）；② **background 领域逻辑下沉 Go**（goja 退役，editor 从内置 App 转平台内置模块）——按模块映射迁移（`project-store`→timeline store、`op-engine`→timeline ops、`model-base`→timeline model、`script-model`/`subtitles`、`project-operations`/`catalog-export`/`subtitle-generate`/`frame-render`→Go op 注册、`components.js`→平台 MaterialService），`recut.editor.*` op 名与 payload 冻结不变，用 `editor_agent_test` 全链路固化为 **golden 一致性套件** + 模块级双跑 + goja 兜底，Go 类型为 schema 单一真相并生成 TS 类型；边界是 Go 只做「变更/校验/读模型」，渲染与关键帧求值留 TS。③ **通信契约版本不动**，只换 UI transport adapter，时间线读写改为 UI/Agent 同走 Go op，`callUI` 仅保留给渲染侧并新增 **Render Host 契约**（`host.ready`/`component.resolve`/`frame.render`/`export.encode`/`probe.render`），把 `@recut/runtime` 共享实例 + import map 从 editor UI 提到平台产物。含素材 `Material{backing: bytes|code}` 标识统一（attrs/content/recipe 后置）、P0–P3 迁移兼容、M0–M4 里程碑与风险（移植漂移、项目数据落点、UI 整份 save、共享纯函数单一 spec、安全沙箱、双 React、App 契约连锁、社区扩展性）。
2026-09-17-reference-understanding.md: 参考理解与克隆执行（单一自包含 RFC；原 clone-skill 已并入）：**全局工具 + 两个全局 skill**。① 工具 `recut.media.probe/frames/contactSheet/boundaries/clip/words/measure`（纯函数与 ffmpeg/Python adapter 分离、可单测、不依赖 Editor/Runtime/生成；默认栈 = Go 编排 + 全局平台 Python venv `<dataDir>/python/platform/<ver>`，在其上增装 ffprobe/PySceneDetect/opencv-headless/Pillow/numpy，`install.sh` 3 份副本需同步；环境铁律：安装时装、升级按版本标记异步重准备、调用时绝不静默装）；② 参考素材基于**真实内容**（`recut.media.reference.create({assetId, sourceUrl?})`，区别于链接型 `create_reference`）；证据写 `metadata.reference`（只装观察），**参考分析写 `attributes`（`ref.*`）+ `content`（可 @ 内联引用证据/World）**——素材 attrs/content 层已实施（`material.go` + MCP `asset.get/update`）；三层归属：观察/参考分析挂素材（跨目标复用）、目标改造留项目；含 AI 写回规范 §3.5；③ **content-first 计划素材**：先建空字节占位素材（`recut.media.asset.create`，待补）写 `content`（「我要它是什么」的规格，可 @），生成时读 content 作提示词、@ 解析为生成参考、产物原位填回同一 assetId；④ **执行工作流 S1–S5 + 五道门禁**（G1 理解/G2 计划/G3 付费/G4 落轨/G5 交付 Done means watched），产物 `metadata.reference`+attrs/content / 占位素材 / 项目 `analysis.md`·`timeline.md`·`clone-plan.md`；验收含**零花费路径**（只换语言+图形文案、不触发生成）；⑤ 全局 skill `recut-reference`（看懂）与 `recut-clone`（跑通），与 remix（决定换什么）分工；词级时间降级为可选（非理解前提）；链接下载不做启动依赖（宿主 Agent 自理为主，需补「本地文件→资产」MCP 入口）。含 M0–M5 里程碑、契约影响与风险。
2026-09-17-asset-attributes.md: 全局素材能力（Material（bytes|code）+ content/attrs，含真实内容参考素材与 MG 全局化迁移）：确立 **Material 为唯一素材载体**（`backing = bytes|code`）——**MG/组件也是素材、不私有于 editor**（同一份 code material 跨 `timeline-editor`/World 画布/remix/AI 短片复用），`timeline-editor` 素材面板 = 全局素材库项目视图、时间线只持引用，**editor 素材 == 全局素材**（`editor_assets` 退回项目引用索引，取代 editor-clone §4 的「素材元素挂 Editor 素材层」），**双击预览 = 全局素材预览编辑框**（`AssetPreviewDialog` 唯一门面：字节 + 可编辑 content/attributes + 来源溯源）。**§8 MG 全局化迁移**：把 MG 从 `apps/editor` 私有 SQLite（`editor_components`/`editor_component_versions`，按项目归属）迁为全局 `component_materials`/`component_material_versions` + 项目引用表（`componentId`/`versionId` 稳定、时间线零重指；`project_id` 降级 `origin_project_id`；存储分 backing；构建/渲染归平台 Render Host；`component.*` op 名不变、写侧切全局、旧表冻结）。**刻意只加两个创作字段**：`metadata.content`（非结构化长正文 + `contentMeta`）与 `metadata.attributes`（有序 typed key-value：text/textarea/number/boolean/select/media/ref/url，`locked` / `source` / `provenance` 字段级溯源：`by/op/jobId/modelId/assetIds/at`；只有 system 能创建锁定字段）；**不引入 facets 命名空间**，系统数据沿用既有 `metadata.proposal` / `metadata.reference`。**核心用法 content-first**：先建空素材 + 写 `content`（富文本说明，可 @ 引用其它素材/实体/World）——参考素材的 content 是「这是什么」，计划素材的 content 是「我要它是什么」；AI 生成时读它作规格、把 @ 解析为生成参考，产物**原位填回同一 assetId**；前置需补 `recut.media.asset.create` 占位入口（当前 `propose` 要求 `capability`+`text`）。**两个服务场景**：① AI 生成字段可溯源；② 导入参考视频获得 AI 内容表达（结构化 attrs + 非结构化 content），并确立三层分离（素材自述 content/attrs ↔ 系统证据 metadata.reference ↔ 目标解释留项目）。接口：`recut.media.asset.get` 读完整视图、`recut.media.asset.update`（`content`/`attrs` 整体替换 / `attrPatch` 按 key 合并，越权 locked fail closed，服务端填 provenance，发 `asset.updated`），系统键由专属 op 写（`reference.attach` / 既有 proposal op）。**解决参考场景错位**：现有 `create_reference` 是公开链接（`kind=reference`/`origin=research`/URL 去重/不抓取）；真实内容参考走**不同接口** `recut.media.reference.create({ assetId, sourceUrl? })`（`kind` 仍为 video、按 `contentHash` 去重、URL 只作溯源、不新建字节）。明确**不引入素材类型目录**。含与 Entity 的对齐/差异表、迁移兼容、M0–M5 里程碑与风险。**M0 + M3 已实施**：`service/media/material.go`（有序 typed attributes + content/contentMeta + locked/attrPatch 校验 + provenance 填充）、`recut.media.asset.get/update`、`PATCH /v1/media/assets/{id}`、`AssetPreviewDialog` 属性/正文编辑、`timeline-editor` 素材面板接入，测试 `service/media_material_test.go`。
2026-09-17-editor-clone.md: Editor 克隆（参考视频 → 素材元素 → 生成 → 时间线）：在 `apps/editor` 内分三层实现克隆，**不新增 XML/DSL 中间语言、不新增平台一等对象**——① Layer 1 参考理解工具落为平台媒体能力（`recut.media.probe/words/boundaries/frames/contactSheet/clip/measure`，纯函数与 ffmpeg adapter 分离、可单测、不依赖 Editor），产出只装观察的 ReferencePack（词级时间可选，非内容理解前提）；② Layer 2 **素材元素 AssetElement** 挂在 Editor 素材层（扩展 `editor_assets`，因 MotionGraphic 也需同套属性）：有序 typed `attrs` + `content` + 确定性 `recipe`（canonical `recipeHash`、typed references、无随机）+ 生命周期 `planned→（触发生成）→proposed→queued→completed`，media 与 MG 同一模型，clip 只引用素材；③ Layer 3 把 clone 收进 `apps/editor/skills/recut-editor`（新增 route 与 `references/clone.md`，决策指向 `recut-director/references/remix`，理解指向全局 `recut-reference`）。注：本 RFC 为总纲，实际按「宿主迁移 → 视频理解 → 素材 attrs 协议层 → clone skill」四步拆分为独立 RFC 推进（迁移见 2026-09-17-editor-native-migration.md）。
2026-09-16-media-generation-proposal.md: 媒体生成提案：把 World Canvas 私有的 `props.proposal` 收敛为**全局媒体资产的生命周期状态**——新增 `media_assets.status='proposed'`（有意向、无字节、无 job、不花钱），`metadata.proposal` 存配方（`references[{id,kind,role,label}]` + 画幅/时长/备注/来源）；`recut.video.generate` 默认只 propose（`mode:"generate"` 为直生逃生门），新增 `recut.media.propose` / `list_proposals` / `update_proposal` / `confirm_proposal` / `reject_proposal` 与 HTTP `POST/GET /v1/media/proposals`、`PATCH /v1/media/assets/{id}/proposal`、`POST …/confirm`、`POST …/reject`；**确认复用同一 assetId** 转 `queued`（画布元素/时间线片段无需重指），AI 可在确认前原地改配方，确认权只在用户（`confirm_proposal` 由 UI/用户触发）；门禁 = video 强制 + 模型目录 `requiresProposal`；素材库插提案卡（确认/放弃）、World Canvas 媒体元素改为引用 proposed 资产（`proposalFromAsset` + `canvas-asset-status` 的 proposed 态，`recut.worlds.proposals.list` 转发资产查询）、Editor 默认先提案；Cover Studio 等低成本图片保持直生不纳入。**已实施**：`service/media/proposals.go` + `types.go` + `jobs.go` 绑定 + HTTP/MCP + `media_proposal_test.go`；`web/lib/media/proposal.ts`（canvas-proposal 转为再导出）+ 素材库/画布前端改造；`recut-worlds` / editor `video-generation` 技能与 ai-short-film（显式 `mode:"generate"`）更新。
2026-09-15-world-canvas-sole-write-surface.md: World 的唯一写入面收口（方案 A，World 即画布）：MCP 只保留只读 + World 生命周期 + 画布接口；内容写入（实体/关系/类型）统一经 `recut.worlds.entity` / `relation` / `entityType`（**不另加 `.canvas` 层**）；语义 CRUD（`entities.upsert`/`create_child`/`promote`、`relations.create`/`update`、`entityTypes.upsert`）下线；补画布路径的三处缺口（一等字段 update、类型 schema、delete/restore）与事件一致（`evidence.archive` 入 `worldMutatingTools`）；含不变式、未动项（App 运行时 capability、revisions/export）与实施/验证清单。
2026-09-15-generation-reference-protocol.md: 生成提示词参考引用协议：把图片/视频生成提示词里的「参考图锚定」（现有工作流写成 `{{Mixed 1}}…`）收敛为统一 `<reference id kind role label />` 标签（取代 09-14 协议 per-type 标签**作为序列化出口**，内部仍保留 `ContextSource` descriptor），新增 role 受控词表（pov/color-card/environment/character/prop/style-ref/motion-ref/voice/sfx/music）与 role↔kind 校验；核心是第一原则「id 的职责是绑定、别名的职责是指称」——同一引用在**创作 Agent（正文 `<reference id>` + sidecar）/ @ 面板（chip）/ 生成模型（`参考图N`、`音频N` 或 `{{Mixed n}}` 别名 + 同序 `referenceIds`）**三种形态下分形渲染；定义确定性 resolver（首次出现编号、组内独立编号、别名模板、referenceIds 顺序、未绑定/role 冲突 fail closed、`referenceMetadata.referenceBindings` sidecar 支持配方回溯）与 M0–M4 里程碑；配套新增全局技能 `recut-directing-generation-prompt`（STYLE LOCK + typed 参考锚点 + 多镜 HARD CUT 连续段 + 声画/负面合同）与 `recut-worlds`（World/Canvas 第一公民 App 的通用操作技能，把世界内媒体生成接到 `brief → 生成提示词 → resolver → 落 media` 流程）；`recut.worlds.brief` 已落地 `references[]` 可引用项表（从实体 media 属性派生，含建议生成 role）与画布生成提案 `canvas-proposal.ts`（结构化 references[] + role 自检 + 视频强制提案）。
 2026-09-15-world-entity-guided-ai-actions.md: World 详情面板的引导提示操作：把详情面板从纯手工编辑器升级为「按主体推荐下一步」的 AI 引导面——① 按预设实体类型（人物/场景/物件/故事/风格/规则 + 通用兜底）在共享 EntityEditor 渲染声明式动作；② 按媒体元素（图片/视频/音频）的**属性名称推断语义**（`inferMediaPurpose`：外貌/服装/表情/三视图/场景全局图/色卡/风格… → purpose + role），在 `ElementPanel` 媒体分支渲染对应动作。动作**九类**（sheet/derive/text/structure/plan/qc/transform/research/sound）、**四种产出**（media 生成 / text 只读建议 / canon-proposal 确认后写 / canvas-proposal），点击后用主体真实数据（attrs/intro/detail/media 属性/属性名 + 世界语境）组装可直接发送的提示词，经 `useAgentPanelContext.setDraft` 交到全局 AI 输入框，绝不自动发送/自动写 Canon；旗舰能力「锚定参考板（Reference Sheet）」把散落设定与既有素材合成一张权威锚定图（角色板/场景板/物件板/风格母版），写回 media 属性并作为后续生成的首选 role 锚点；含完整动作目录与人物/场景/媒体预填示例、属性名→purpose/role 推断词表、requires/rank 自适应、只读世界与窄屏策略、纯前端注册表（`web/lib/world-entity/guided-actions.ts` + `guided-media-actions.ts`）与类型级 `ai_actions_json`（M3）演进、M0–M4 里程碑与验收。**M0/M1 已实施（2026-09-15）**：纯内核收敛在 `web/lib/world-entity/guided/`（types/media-purpose/context/entity-actions/media-actions/registry + 单测），`GuidedAiSection` 接入 `EntityEditor` 与 `ElementPanel` 媒体分支，经 `setDraft` 交全局输入框；`tsc` 零错、`npm test` 30/30。
 2026-09-14-unified-context-mention-panel.md: 全局 Chat 的「统一上下文引入面板」：把 @ 从单一素材下拉重构为可 Group / 可搜索 / 可按类型过滤的上下文目录（对齐全局模型设置 ModelPicker 的左虚拟列表 + 右预览双栏）；统一九类来源——当前 page（Work Surface）、当前 selection（Work Focus，按 ref 展开为具体引用）、World、World Entity、World evidence、项目、应用、素材、Skill、MCP 工具；定义 `ContextSource` 目录组、`ContextOption`/`ContextPreview` 契约、搜索合并与五级排序规则、Entity/Evidence 的 World scope 二级导航、inline/attach 两种插入模式、`allowedRefTypes` 裁剪、Portal 响应式与降级；后端 `contextMaterializers` 新增 `world_evidence/project/app/skill/mcp_tool` 与对应 XML 标签。**与同日内联协议 RFC 合并为同一份注册表与单一里程碑**（本 RFC 负责 M0b 目录内核/M1 面板 UI/M4 来源，协议 RFC 负责 M0a/M2/M3/M5/M6）；面板须可被裁剪且不假设只在 Chat（Entity 简介/正文只用 L1 轻菜单，不加载本面板）。
2026-09-14-rich-context-composer-protocol.md: 富文本上下文输入协议：把 brainloop `editors/` 的 Tiptap 原子 mention + markdown/XML 双向协议搬运进 Recut 并**通用化**为全站复用的 `RichComposer` 内核——**分三层**（L0 无 UI 的协议/扩展/value、L1 React 组件 plain|referencing 双模式 + composer|field|inline 三变体、L2 目录+面板按需加载）；引用类型注册表 `ContextSource`（协议组归本 RFC、目录组归选择面 RFC）抽出硬编码的 source/skill/connection；输入框内联 chip（图标+名称+删除/跳转）、`@` 触发菜单、序列化为 `<media assetid>`/`<creation_world worldid>`/`<creation_entity worldid entityid>`/`<world_evidence>`/`<project>`/`<app>`/`<skill>`/`<mcp_tool>`/`<context-quote>` 自闭合 XML 供 AI 直接识别并调 MCP 工具取真相；**受控 `RichComposerValue{text,refs,doc,isEmpty}` 可替换任意 textarea**，首批复用目标是 World Entity 简介/正文/长文本 attr（无 contexts、不走后端、只存 markdown+XML，证明通用性）；内联标签为**主锚点**、既有 `contexts` 旁路保留为 Chat 专用校验/媒体物化通道，二者以 descriptor `identity` 保证 ID 一致，后端补 `inline_ref` 归并提示段防漏传；同一 registry/parse 同时驱动输入侧 chip、回复侧卡片与非 Chat 只读展示；合并后单一里程碑 M0a 内核协议→M0b 目录内核→M1 面板→M2 输入内核(两处落地)→M3 双轨对齐→M4 工作台/能力来源→M5 回复复用→M6 全站铺开。含 brainloop 盘点、XML 语法与命名规范、迁移兼容、测试点与未决问题。
2026-09-13-world-content-format-v2.md: World 内容格式 v2：把 PGC 与 UGC 收敛到同一套源格式与 manifest——**一个对象一个文件**（`world.json` 索引 + `entities/<id>.json` 统一实体 + `assets/<id>.json` 素材协议（file/kind/recipe 生成配方）+ `canvas.json` 只读画布 + world.md + references/examples），发布为单文件 manifest v2（内联 canvas、媒体镜像 CDN 绝对 URL）；物化 v2 直接落 `world_entity_types`/`world_entities`/`world_relations`/**`world_canvases`**（修复现物化不写画布、把 evidence 写进已判废 `world_asset_refs` 的有损映射）；媒体统一为 media attr `{assetId|url}` 并收敛到单点 `resolveMediaSrc`（asset→媒体库流 / 应用内 url→`/v1/files/remote` 同源代理保纹理可读 / 官网 url→直连）；管理模型为「创作面 in-app → 交换单位源 bundle（worlds.export/import，全文件不绑 assetId）→ 分发单位 CDN Catalog（platform 自动同步 / published P4）」，差异只在 origin 与信任；只读画布预览补齐交互契约（可导航/选中/进入/预览，禁一切写）与官网静态画布投影。**已落地**：service manifest v2 物化（含 canvases，保留 v1 路径）、`web/lib/world-media.ts` 单点解析、`scripts/worlds-migrate-v2.mjs`（8 个世界已迁 + 自动画布）、`worlds-publish.mjs` v2 + seed、官网静态画布预览、**export/import 功能**（service `worlds_bundle.go` + HTTP `/v1/worlds/{id}/export`、`/v1/worlds/import` + 工作台「导入 World / 导出为 zip」UI，素材内嵌且按内容哈希去重）；待办：MCP 暴露、publish（`pub.*`）。
2026-09-13-vello-native-rendering-implementation.md: World Canvas 从 pixi v7 迁移到 vello-native 单栈的**实施 RFC**：单 wgpu 上下文内 vello 负责矢量光栅、自建 instanced-quad 合成 pass 贴瓦片、Overlay 走 DOM/SVG；逐模块逐常量移植 open-pencil 的 production 瓦片系统（256 设备像素瓦片 / 0.25 LOD 步长 / 5ms·32job 预算 / 双代失效 / 128MB LRU / mandatory·visible·overscan 优先级 / navigationActive defer）；含 chunk 局部坐标 + per-chunk affine（position 失效 O(1)）、瓦片 bleed 消缝、screen-constant 元素进 Overlay、显存预算、导出整场渲染、模块文件清单与 M0–M4 决策门。自审修正 10 项（禁 vello draw_image 合成、v1 用 classic、atomic chunk 等）。
2026-09-13-vello-tile-renderer-research.md: Vello × open-pencil 瓦片策略 × pomelo-vello-adapter 的调研报告：核验 open-pencil 瓦片源码（scheduler/planner/cache/render/controller 逐文件）、recut 现有 pixi demand-flush+M1 快照、mello 前序 vello 尝试与踩坑、react-vello 边界范式；关键判断「vello 缓存是 drawcall/编码级、Skia 是栅格级，故非等量替换」，并给出缓存粒度对比、组合形态与硬约束（双 API 纹理共享成本）与证据索引。决策：vello-native 单栈。
2026-09-09-unified-entity-model.md: 统一 Entity 模型：Entity = 名称/介绍/详情/属性列表/类型 + 双向关系图；attrs 有序 typed key-value（preset locked 字段 + 用户可扩展），素材统一为 media attr 并废弃 evidence 层（含「参考」类型），删除 kind enum、content_json 散装字段与设定视图硬编码 fieldDefinitions，设定视图与画布共享一套 EntityEditor；含迁移脚本与 P0-P3 落地计划。
2026-09-09-world-canvas-document-storage.md: World Canvas 存储粒度重构：一张画布 = 一个 Document（world_canvases 文档表，context_id 区分根/内层，doc_json 不透明正文 + version 乐观锁），内层画布独立成档、实体/关系语义层保持共享；解决实体投影卡跨层复用一行导致「内层调位置写穿外层」的结构问题；含 get/save/docs/doc.update 读写契约（存储文档粒度、操作面保留元素粒度 ops）、文档粒度取舍记录（写放大/并发/attr 投影同步对策）与 M1-M4 迁移回滚。
2026-09-08-canvas-pixi-tiles-implementation.md: 方案二落地文档（上游 RFC 0831）：不改上层调用体验、全部收敛在 pomelo pixi adapter/ticker 层的瓦片渲染实施设计——demand-driven flush（autoStart:false + 脏标记合并每帧一次 GPU 提交）、dev/demo-pencil 算法验证页（TileScheduler 直译常量与优先级/双代取消/EMA 成本/RT 池）、M1 拖拽会话全屏快照（beginContentSession(excludedBlockIds) 把受影响元素问题折成插件传参）、M2 瓦片调度（块级 chunk RT + 瓦片拼贴 + 5ms 预算 + LRU 显存常数化 ~25-30MB）、落地文件清单与风险逃生门。
2026-09-08-canvas-rendering-skia-vs-tiles.md: World 画布渲染引擎升级路径对比：完整迁移 open-pencil（CanvasKit/Skia）core 渲染层进 pomelo（方案一）对比保留 pixi v7 并吸收其 tile 系统思想（方案二）；读码梳理 open-pencil 四层还原缓存（SkPicture/retained backing/TiledSceneController+TileScheduler 5ms 帧预算+positionPreview 拖拽机制），给出方案二起步、方案一留档的决策建议与 M0-M3 里程碑（dev/demo-pencil 验证页起步）、GPU ≤3ms 验收口径。
2026-09-07-recursive-world-canvas.md: Recut World 无限画布演化：把 World 从表单设定本升级为递归语义世界画布（Entity 即可进入容器、Reference≠Relationship、Exploration/Canonical 分层、tldraw 投影）；数据结构按「基石」从零设计、不为早期用户做兼容约束——可扩展 entity type 目录（world_entity_types 预设+自定义+base_kind）、递归容器（parent_id/is_provisional）、局部子图（scope_entity_id）、world_canvas 承载「Entity 骨干+自由表达元素（文本/图片/形状/便签）」、坚持「边≠关系」（语义关系只存 world_relations）；含 MVP 执行计划（约 6 天：骨架→数据→递归容器→画布→Promote 闭环）。
2026-08-12-creation-worlds.md: Creation Worlds 顶级工作台、创作上下文绑定、版本化 Canon、跨 App / MCP 契约与 Remotion MVP 的实施 RFC。
2026-08-12-creation-worlds-technical-design.md: Creation Worlds 的 workspace SQLite、Go service facade、SDK/MCP、权限、Web/Remotion 接入、迁移与测试的技术实施 RFC。
2026-08-13-visual-runtime-component-system.md: Recut Visual Runtime 与 Component System：世界（场景图）+ 时间线创作表面、R3F 全权渲染、组件即代码对象、材质混合与排序规则、Preview/Export 统一与分阶段实施路线。
2026-08-14-editor-data-model-selection.md: 编辑器数据模型（DocumentData + EditorState + Ephemeral Layer + NodeState）与选区/元素定位架构：单一解析入口、实时渲染几何 bbox、Model API（含关键帧提交策略）、渲染路径收缩与 Chromium 自测方案。
2026-08-14-realtime-channel-ws.md: 平台实时通道的单 WS 收敛：一条长连接 + channels 订阅 + 心跳保活 + REST 首屏取数 + 单后台账本转发，以及 iframe App 的宿主桥/直连 WS 双链路传输抽象。
2026-08-14-ai-temp-components.md: AI 临时组件（Temp Components）：代码即项目内临时素材。surface 作者分级（html/react/r3f）、source+bundle 同版本持有、head 跟随语义、@recut/runtime 托管 SDK 与服务端构建工具链、安全边界与验证闭环。
2026-08-14-editor-ai-agent-surface.md: recut.editor 面向 AI 的完整 MCP 工具面与 Skill 契约。操作驱动取代文档驱动；Headless 共享 Model API 单写入口；统一 op 日志（snapshot undo 单一权威）；aiLock 会话锁 + baseVersion 乐观并发；版本号/op 广播双档前端同步；preview.frame 视觉验证闭环；headless 双模导出；SKILL.md + references 分层技能与端到端验证方案（L0-L4）。
2026-08-14-creation-worlds-product-reframe.md: Creation Worlds 面向完整用户产品的重构 RFC：以创作设定替代 Geek 数据表面，定义类型化编辑、完整生命周期、AI WorldBrief 与需确认的写回契约，并规划旧 Canon 的渐进迁移。
2026-08-15-editor-chatcut-adoptions.md: recut.editor 系统性吸收 ChatCut 对标结论：script-first 文稿剪辑面（script.read/apply/clean）、speech-track 转录来源、track role 自动 duck + audio.smooth、catalog-first 内置效果/SFX 目录、skill 工艺层（speech-editing/subject-protection/errors references）与验证/生成纪律，并明确不采纳边界（保留 op 日志、确定性关键帧、preview==export、本地渲染）。
2026-08-16-marketing-seo-social-share.md: 官网 SEO 与社交分享完整方案：逐路由元数据与默认 noindex、canonical/域名收敛（www 301、/marketing 别名、软 404）、robots.txt/sitemap.xml、Open Graph/Twitter Card 与微信/微博/X/LinkedIn 等平台分享预览、OG 图片静态生成、JSON-LD 结构化数据、工作台 noindex 隔离、Worker 边缘调整、图标集与 Blog 内容补强。
2026-08-16-i18n-zh-en.md: 全站多语言（zh/en）整体方案：语言枚举与两种语言真相（官网 URL 驱动、工作台/Service/App 偏好驱动）、官网多路由与 Worker 重写映射、逐语言 metadata/hreflang/sitemap/canonical、内容模型 locale-keyed、轻量类型化字典、工作台偏好端点与 Accept-Language 传递、manifest.localized 契约、ctx.locale 注入、iframe 语言注入与 Agent guide/MCP 语言一致化。
2026-08-16-marketing-content-plan.md: 视频创作主题域内容计划：AI 视频生成/视频制作科普/工具文/创作方法论四个主题簇的选题库（Google 全球 + Google 中文视角）、内容形态配比（权威 60% / 承接 40%）、zh/en 双语言生产模型（对齐 i18n RFC）、P0–P2 执行顺序与榜单时效更新机制；P0 打样第一篇为「AI 视频生成后怎么剪辑」。
2026-08-16-i18n-zh-en.md: 全站 zh / en 双语言整体方案：两种语言真相（官网 URL 驱动、工作台/Service/App 偏好驱动）、官网多路由与 Worker 重写映射、逐语言 metadata/hreflang/sitemap/canonical、`lib/i18n` 类型化字典、`/v1/preferences` 偏好端点、Service 的 Accept-Language 传递、manifest `localized` 契约、`ctx.locale` 注入 background.js、iframe `?locale=` + 宿主桥 page.context.locale、Agent guide/MCP 语言一致化。
2026-08-16-editor-font-system.md: 编辑器字体系统：Recut 自有 CDN 自托管字体（cdn/fonts/google，fetch-fonts.mjs 编制上传，运行期零 Google 依赖）、service /v1/fonts 目录与字体文件 API（首次从自家 CDN 抓取 + 内容寻址缓存 + 离线可用）、Google Fonts / Local Fonts 双 Tab 面板、中文 CJK 家族与 unicode-range 按需加载、本机字体枚举（queryLocalFonts + SYSTEM_FONTS 扩充）、用户上传字体（FontFace 注册 + 持久化）、渲染/导出一致性，以及基于 Playwright 的分层 E2E（L1 Go httptest / L2 编辑器 fixtures mock / L3 全链路）。
2026-08-16-canonical-assets-opfs-cache.md: Canonical Assets：用户设备上 Recut Assets Service 是图片/视频/音频唯一真相源，App 经 `recut.assets` SDK 获取/上传素材，OPFS/IndexedDB 是按 `(assetId, contentHash)` 可重建缓存；规定跨域名/跨设备按需同步、渐进 Loading、离线状态、删除墓碑（“资源已删除”）与分阶段验收。
2026-08-16-agent-work-surface-context.md: Agent Work Surface Context：将当前页面从自由文本附件升级为宿主签发的目标绑定，并与 App 局部 Focus 分层；定义项目/World/素材/App 全页面语义、用户引导、领域 Skill 提示、迁移、测试与可观测性，不改变既有权限范围。
2026-08-16-editor-component-asset-workflow.md: 编辑器组件素材工作流：组件先入库、默认不落轨；模板化快速创建与平台可信验证；MCP/UI 统一时间线避碰 placement，并以原子批量放置消除同轨重叠。
2026-08-18-remotion-studio-music-font-finetunes.md: remotion-studio 音乐与字体微调：复用 Recut CDN（audio/fonts catalog 与编辑器同一份数据）与 catalog-first 架构，不做代码复用；音乐「选择即导入」为媒体资产走 composition.assets 物化管道、字体经 CDN 自托管 {id}.css 注入（caption 主题 palette.font 覆盖）、预览静音门控，MusicFineTune/FontFineTune 两模块与 Prompt 契约、选择持久化与 workflow.context 资源可见性，及分阶段实施与分层验证。
2026-08-18-subagent-task-card-ws.md: 子 Agent 任务卡片与全局预览：核心原则是子 Agent 与通用 Agent 会话同构（落 agent_sessions/agent_events 账本、复用 handleCodexEvent 事件解析与 agent channel），差异仅在受限工具面——执行一次性但记录持久化、可审计、可实时观察（修正"stdout 丢弃、无账本"的设计缺陷）。chat 工具事件携带 subagentId 判别字段（服务端注册表注入 tool.completed payload）→ 前端自动渲染任务卡片（状态/阶段/实时耗时 counter）→ 点击弹全局视图（本质就是复用 Conversation 渲染子会话 + subagent Meta 头）；子会话活动走 agent channel(childSessionId)、job 生命周期走新增 subagent channel(jobId)（账本回放、终态 available:false）。含 schema 迁移、三阶段 phase、REST cancel、审计链 parent→job→child，及"向子 Agent 发消息"的交互边界（同会话续谈 Phase 2 可行，运行中插话不可行）。
2026-08-18-editor-component-create-trace-issues.md: 第二次「Hello React 组件」创建链路事故的问题梳理：组件 verified 但素材库不显示（排版分两区 → 已合并单一网格）、`<app appid>` 引用标签在 prose 中被渲染成卡片、`recut.job.wait` 同步长轮询触发 MCP EOF、工具结果预览把 `component:` 资产当媒体资产导致永久 Loading、组件卡片无预览图（封面 harness 离屏 iframe 导致 rAF 挂死 → 已修复并验证，含复用 WorldRenderer 渲染路径的评估）。
2026-08-19-editor-component-create-resilience-and-compositing.md: 六组件产品介绍会话（7a13e308）复盘与架构修复方案，四堵墙：单一事实源（job 事件日志 + 状态机投影，终态一律 finalize）、边界分层（作者契约/错误面/验证权杖各归其位）、平台拥有框架模型只拥有内容（脚手架生成 + 结构化契约 + 构建闸=运行安全非类型卫生）、证据驱动验证（信任阶梯 build/render/composite + 证据戳）。覆盖 6 个问题：①90s 超时 + create 不抗杀（**已决策超时 90s→30min** + `recut.job.cancel` + 事件日志 + 杀后仍 finalize）；②作者契约与 strict typecheck 失配（脚手架自带类型、typecheck 降级，消除整批 TS7006）；③MCP 错误面（统一错误信封 kind/code/hint、终态即结果、wait 改事件订阅）；④失败账本不可见（事件日志 SSOT，一切视图是投影）；⑤上下文层偏离（session digest 投影 + 合成上下文平台推导注入契约，默认透明）；⑥快速路径验证是代码级自证（统一验证管线 + 证据戳，取消 update 自证）。
2026-08-19-platform-communication-op-bus.md: 平台通讯架构：把 WS/HTTP 桥/MCP/shell job/事件账本五套方言收敛为统一 Op 总线契约——标准信封（id/correlationId/from/to/version）、对称原语（on/call/publish/handle）、统一异步 Handle（async_ops，shell/media/deferred 三类统一由 recut.job.* 观察）、App→UI RPC（ctx.project.callUI + rpc.reply + iframe recut.on）。preview.frame 是首个验收消费者：Agent 一次调用返回 jobId，recut.job.wait 直接拿 {imageUrl}，无专用轮询/请求表。稳定契约见 docs/platform-comms-contract.md。

2026-08-19-editor-ai-video-authoring-quality.md: Editor AI 成片质量重构：实际对照 ChatCut plugin 0.2.21 的 scenario/router、A-roll/B-roll/MG、生成镜头与 verification skills，从文字时间线拼接升级为 route + treatment 编排；定义好视频的叙事、画面、镜头关系、视觉系统、节奏与交付证据，规定 CreativePlan、设计回执、依赖图、组件准入、原子 transaction、Op 总线增量同步、用户中断收敛和预览/导出门。
2026-08-20-editor-component-gsap-animation.md: 组件动画升级：GSAP Timeline + 运行时逐帧 seek(t) 作为 react/r3f 承载面主要动画模型（html 保持 anim.*）。确定性从「每帧渲染纯函数」演化为「构造确定性 + 驱动靠 seek」；r3f 复用同一 useTimeline/useGSAP 模式（目标是 Object3D ref）；useTimeline/useFrameContext API + 插件白名单 + 构建期确定性扫描扩展；gsap-skills 融合进 references/gsap.md 与作者契约；零强制迁移。
2026-08-22-editor-animation-presets.md: 剪映式预设计动画方案：动画素材目录、入场/出场/循环处方、动画层与手工关键帧组合、文本字/词/行分段动画、预览/缓存/权限、统一 timeline op 与 Preview==Export 验收。
2026-08-22-editor-animation-runtime-architecture.md: 动画本质层架构：GSAP 作为统一 seekable timeline，DOM/React 文本、Three Object3D Transform、Shader Material Uniform 通过目标适配器接入；预设只是可序列化 Motion Program，Preview 与 Export 共用同一求值时序。本 RFC 细化并优先于上一份预设产品 RFC 的底层设计。
2026-08-23-editor-effect-graph.md: Element Effect Graph：复用已验证的 components/effects Shader pass，EffectDefinition 支持多实现，Preset/Effect 统一编译为可组合 Graph，并按 texture/material/geometry/composite 能力选择渲染路径。
2026-08-24-skymind-token-api-provider.md: Skymind Token API Provider（GPT Image 2 图片 + Seedance 2.0/2.5 视频）与平台级临时公网分享：复用 Provider Registry 五层结构新增 `skymind-token`（统一任务协议 + 预扣费 checkpoint 幂等）、参考素材经 R2 `share/` 前缀 7 天 TTL 公网分享、模型 ID 凭据级可覆盖（视频模型不可发现）。**截至 2026-08-25 P0 已实施**：providers/skymind 适配器、catalog 三模型、jobs_skymind.go、shares.go（media_shares 账本 + 自研 SigV4 客户端）、model_overrides_json 迁移、REST 分享 API、Settings 卡片与 i18n；经平台全链路真实 E2E 验证（图→自动发布公网 URL→Seedance 2.5 参考图生视频→Seedance 2.0 文生视频，产物字节有效 + usage/task id 落 Asset + 分享吊销），P1（独立桶/域 + MCP + 分享 UI）/P2（TTS 等端点族）未实施。

2026-08-21-ai-narration-audio-asset-lifecycle.md: AI 解说音频素材生命周期：先梳理 4 个 UX 缺陷（Audio Studio 生成时 recut.job.wait 触发 Streamable HTTP EOF、AI 落轨配音 sourceType/sourceUrl/mediaId 混用致时间线无声、new-authoring 对说明性输入未默认 voice-led 且缺 B-roll 验收、Settings 缺本地 TTS 作语音 Route/默认项），再给落地方案细化并把改动分为 A 类代码修复（契约文案对齐 + errors.md runbook、route 表 voice-led 默认 + Visual-first sync map 硬产出）与 B 类架构设计（项目级 audio asset + timeline.placeAudio + validate 可播放性，editor_assets 扩 audio 类型并扩展编辑器媒体供给面；本地 TTS 经 MediaRoute.CredentialID 允许空 + execute 本地分支 + audio-studio 桥接 op 入语音路由并支持云端回退）。给出云图、数据模型、op 契约、验收标准与 P0/P1/P2 实施顺序。截至 2026-08-21 已实施 #1/#2/#3/#4：recut.job.wait 服务端 15s 封顶 + 文档、placeAudio + audio-unresolvable + project.assets.changed 前端实时同步、voice-assets.md 声音资产总览、local-audio 本机 TTS provider（catalog/无凭据路由/readiness/默认音 + wireLocalSpeechBridge 执行桥），均带 API 级端到端测试。
2026-08-22-editor-captions-audio-studio-asr.md: 编辑器字幕「生成」对接 audio-studio ASR，以**通用能力桥**（capability bridge）为地基：平台不做具体业务能力，只做能力发现（ctx.platform.integrations + manifest capability）、通用跨 App 调用（ctx.capabilities.invoke，复用既有 AppHost.invoke/InvokeMCP）、统一异步 Handle（async_ops + recut.job.*）与授权/审计/scope 传递，并给出完整的**通用层健壮性与容错契约**（统一错误信封、三档超时、重试/幂等、取消传播、级联故障、授权签名与越权拦截、单作业背压、观察一致性、部分成功 repair 路径、审计账本），配故障注入测试矩阵；转写是 audio-studio 的能力（audio.transcribe 扩 `saveToLibrary` 开关，编辑器**一次调用**完成转写+授权入库），产物默认入库全局 assets、项目只持引用（track.captionSource + cue 由资产派生 + script.attach 同源文稿）；并定义 local_speech_bridge 这类业务执行桥的技术债收敛方向。**截至 2026-08-22 P0 已实施**：service ctx.capabilities.invoke/inspect（错误信封/同步超时/审计）+测试、audio-studio saveToLibrary+懒终态入库+去重、editor subtitle.generate/status/commit/capabilities + captionSource + UI 生成链路、宿主全局 AppInstallGuide 统一安装引导弹窗 + iframe apps.request-install；web tsc 零错、service go test 全绿、editor vite build 通过。

2026-08-28-world-onboarding.md: World Onboarding 场景化引导：创建后从"你手里有什么"出发——五份内置场景蓝图（小说改编 / IP 账号 / 风格表达 / 品牌手册 / 从零开始）替用户思考世界结构，四条素材注入路径（上传 / 链接 research / 素材库 / 口述追问），Agent 标准完善工作流（readiness → research → generate → proposal → 确认写回）与候选素材画廊审视；服务端 Readiness 完备度投影（skeleton/draft/ready + 有序 missing）同源填充 brief.missing 与详情页引导卡；移除空壳模板实体的完成假象，propose/apply 服务端门禁仍归 08-14 重构 Phase 2。

2026-08-28-pgc-platform-worlds.md: PGC 平台 World 内容层：单一 World Catalog（kind=platform 自动同步 / kind=published 手动安装，均发布平台 CDN；materialize/archive 统一原语，UGC 扩展位冻结待 P4 World Store）、World 核心技能模型（world.md 一等公民：独立 Tab、内联 brief、进 revision；实体 body 长文；不设 guide 类型）、源格式（worlds/<slug>/ 目录内聚 world.json + world.md + references/ + 资源，构建期 $file 解析与 CDN 镜像）与发布格式（单文件自包含 manifest，SHA-256 校验）、origin 三元来源（local/platform/published）与只读+Fork 写策略、Evidence assetId|url 双源与 recut.media.import_url、recut.worlds.brief v1 单次读取入口；以 helloianneo/ian-xiaohei-illustrations 小黑仓库为首个实例（@小黑 → shot list → 系列图生成）。

此目录保存尚未实施或分阶段实施的平台设计决策。RFC 定义目标、边界、数据与接口契约；获批实现后，代码与运行时文档必须反向更新以保持一致。

[PROTOCOL]: 变更时更新此头部，然后检查 README.md
