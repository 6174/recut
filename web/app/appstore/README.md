# appstore/

> L2 | 父级: /web/app/README.md

成员清单
[page.tsx]: `/appstore` 的兼容路由壳；挂载共享工作台并激活 Apps。
说明: Apps 已成为独立顶级入口；保留 `/appstore` 兼容既有深链，应用详情路由仍为 `/apps/[appID]`，由 apps/ 目录维护。

[PROTOCOL]: 变更时更新此头部，然后检查 README.md
