# appstore/

> L2 | 父级: /web/app/README.md

成员清单
[page.tsx]: `/appstore` 应用市场的路由壳；挂载共享工作台并激活 Appstore Tab，展示已安装 App 与市场条目。
说明: 原 `/apps` 应用目录已更名为应用市场，保留 `/apps` 为到 `/appstore` 的兼容重定向；应用详情路由仍为 `/apps/[appID]`，由 apps/ 目录维护。

[PROTOCOL]: 变更时更新此头部，然后检查 README.md
